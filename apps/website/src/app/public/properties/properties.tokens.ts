/**
 * Fallback slug used by pages that have not yet been migrated to read the
 * active property from PropertyContextService. Lets existing single-property
 * pages keep working during the multi-property re-rooting.
 */
export const DEFAULT_PROPERTY_SLUG = 'teddy-city-enugu';
